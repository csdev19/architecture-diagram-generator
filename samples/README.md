# Sample JSONs

Validated Diagram Document v2 examples for opening in the editor.

- `diagram-tool-layers.json` — this repository, organised by layer.
- `kaipu-layers.json` — Kaipu Record, organised by layer.
- `trip-planner-layers.json` — Trip Planner, organised by layer.
