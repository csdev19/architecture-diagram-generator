// Database enum values will be added here.
//
// `export {}` makes this a module rather than an empty file, which is what the
// `no-empty-file` lint rule objects to. It printed a warning on every lint run
// and every push, which is how a warning stops being read.
export {};
