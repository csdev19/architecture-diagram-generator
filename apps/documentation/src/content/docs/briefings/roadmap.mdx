---
title: Roadmap
description: What is built, what is in review, and what is committed but not started.
---

# Roadmap

**Last reviewed: 2026-09-02.** Status was determined from pull request state —
`gh pr list --state merged` and `--state open` — not from the plan documents in
`docs/plans/`, which record intent and lag reality. There are no git tags and no
releases, so PR state is the only ground truth here.

## Shipped

Six pull requests, all merged, all on 2026-09-01 and 2026-09-02. Everything
below is on `main`.

| Capability               | What it means                                                                                                                                                                             | Landed in |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| Monorepo customised      | The template reduced to the "fullstack serverFn only" pattern — one TanStack Start app on Workers, no API Worker — with the architecture and docs rewritten to match                      | PR #1     |
| The diagram contract     | `DiagramConfig` in `packages/domain`: the Zod schema, its author-facing error messages, the geometry and palette constants, and the authoring guidelines string                           | PR #2     |
| The renderer             | `renderSVG` — a pure, deterministic string builder with no DOM or Node APIs, so it can run unchanged inside a Worker                                                                      | PR #2     |
| The `/editor` page       | Two panes: JSON on the left, live preview on the right, with SVG and 2x PNG export done in the browser                                                                                    | PR #2     |
| Brand logos              | A 20-key `iconKey` registry built on `simple-icons`, plus a WCAG contrast rule that resolves a legible fill per tile. Technologies with no entry keep an emoji                            | PR #3     |
| Visual editing           | Tiles drag and snap, a click opens an inspector, edges are drawn by clicking two tiles, and **Arrange** re-places nodes from the topology — every edit a text-to-text rewrite of the JSON | PR #5     |
| Rasterisation, de-risked | Proof that `@resvg/resvg-wasm` initialises and renders inside the Worker, behind a throwaway `/api/render-spike` route                                                                    | PR #4     |
| These briefings          | The set you are reading, and the binding file that regenerates it                                                                                                                         | PR #6     |

## In flight

**Nothing.** No pull requests are open and no branch is in review as of the
review date above. The next person to touch this repo starts from a clean
`main`.

## Next

Committed direction, not started. This is the whole of it — there is no backlog
beyond this list, and nothing here has a date.

### Finish the server-side render and the MCP server

`docs/plans/2026-09-01-diagram-tool-next-phases-02-api-mcp.md`, **Tasks 2–6**.
Task 1 is done and merged; the rest was paused by an explicit decision on
2026-09-01 in favour of building the visual editor first.

It matters because it is the half of the original thesis that does not exist
yet: today a person pastes a config into the editor by hand, and the point of
the project is that a model hands one over directly. What remains is font
embedding, an R2 store with REST endpoints, loading a saved diagram by id in the
editor, the MCP endpoint itself, and deployment.

The spike already settled the question that gated all of it. Cloudflare Workers
refuse runtime WebAssembly compilation, so the wasm must be a **bundled import**
compiled at deploy — fetching the binary and instantiating it fails with
"Wasm code generation disallowed by embedder". The assets binding stays viable
for data, which is how fonts will load, but never for code. Two further findings
are written up in that plan file: Vite's dependency scanner misreports the
`.wasm` specifier failure as a stale React pre-bundle, and `wrangler dev` is the
only honest answer to a Worker-runtime question, because `vite dev` never gets
far enough to ask it.

Two of the remaining tasks cannot be done from the repo alone: the store needs a
Cloudflare account with a bucket and a secret, and registering the MCP server as
a connector is a manual step in the Claude interface. That is why the phase was
paused rather than pushed through.

### Known smaller gaps

Recorded so they are not rediscovered.

- **Auto-layout ignores groups.** `layoutDiagram` re-places nodes and resizes the
  canvas but returns group boxes untouched, so **Arrange** breaks a
  hand-composed grouped diagram. It would need to recompute each group's box
  from the nodes it contains.
- **Server-side rendering draws no text yet.** The spike rasterises with
  `loadSystemFonts: false` and no font buffers, so shapes and logos render and
  glyphs do not. That is exactly what Task 2 fixes.
- **`/api/render-spike` is throwaway** and says so in its own source. It is
  deleted when Task 3 introduces `/api/render`.
- **The icon registry has 20 keys.** Anything outside it falls back to an emoji,
  which works as designed but is the obvious thing to grow with use.
